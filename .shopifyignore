# Atomic-write temp files created by editors/tools. The theme dev watcher would
# otherwise try to upload them, and Shopify rejects the generated filenames.
*.tmp.*
*.swp
*~

# `sed -i` writes sedXXXXXX alongside the target before renaming it into place.
sed??????
sed*
